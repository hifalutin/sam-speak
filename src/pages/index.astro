---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Sam Speak</title>
	</head>
	<body>
		<div class="container">
			<h1>Sam Speak</h1>
			<div class="grid">
				<div class="rectangle" data-number="1">1</div>
				<div class="rectangle" data-number="2">2</div>
				<div class="rectangle" data-number="3">3</div>
				<div class="rectangle" data-number="4">4</div>
				<div class="rectangle" data-number="5">5</div>
				<div class="rectangle" data-number="6">6</div>
			</div>
		</div>
	</body>
</html>

<script>
	// Initialize Web Speech API
	const synth = window.speechSynthesis;

	// Function to speak a number
	function speakNumber(number: string) {
		// Cancel any ongoing speech
		synth.cancel();

		// Create a new speech utterance
		const utterance = new SpeechSynthesisUtterance(number);

		// Set language to US English
		utterance.lang = 'en-US';

		// Configure voice settings for more natural speech
		utterance.rate = 0.9; // Slightly slower for clarity
		utterance.pitch = 1.0;
		utterance.volume = 1.0;

		// Try to use a high-quality English voice if available
		const voices = synth.getVoices();
		const preferredVoice = voices.find(voice =>
			voice.lang.startsWith('en') && (
				voice.name.includes('Google') ||
				voice.name.includes('Enhanced') ||
				voice.name.includes('Premium') ||
				voice.name.includes('Samantha') ||
				voice.name.includes('Karen') ||
				voice.name.includes('Natural')
			)
		);

		if (preferredVoice) {
			utterance.voice = preferredVoice;
		}

		// Speak the number
		synth.speak(utterance);
	}

	// Add click event listeners to all rectangles
	document.addEventListener('DOMContentLoaded', () => {
		// Load voices (they may not be immediately available)
		if (synth.getVoices().length === 0) {
			synth.addEventListener('voiceschanged', () => {
				console.log('Voices loaded');
			});
		}

		const rectangles = document.querySelectorAll('.rectangle');
		rectangles.forEach((rectangle) => {
			rectangle.addEventListener('click', () => {
				const number = rectangle.getAttribute('data-number');
				if (number) {
					speakNumber(number);
				}
			});
		});
	});
</script>

<style>
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	body {
		font-family: Arial, sans-serif;
		padding: 20px;
		background-color: #f5f5f5;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	h1 {
		text-align: center;
		margin-bottom: 30px;
		color: #333;
	}

	.grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 20px;
	}

	.rectangle {
		background-color: #3498db;
		border-radius: 8px;
		padding: 40px;
		min-height: 200px;
		display: flex;
		align-items: center;
		justify-content: center;
		color: white;
		font-size: 24px;
		font-weight: bold;
		transition: transform 0.3s ease;
		cursor: pointer;
		user-select: none;
	}

	.rectangle:hover {
		transform: translateY(-5px);
		box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
	}

	.rectangle:nth-child(1) { background-color: #3498db; }
	.rectangle:nth-child(2) { background-color: #2ecc71; }
	.rectangle:nth-child(3) { background-color: #e74c3c; }
	.rectangle:nth-child(4) { background-color: #f39c12; }
	.rectangle:nth-child(5) { background-color: #9b59b6; }
	.rectangle:nth-child(6) { background-color: #1abc9c; }

	@media (max-width: 768px) {
		.grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (max-width: 480px) {
		.grid {
			grid-template-columns: 1fr;
		}
	}
</style>
